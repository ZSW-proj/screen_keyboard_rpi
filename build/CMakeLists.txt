cmake_minimum_required(VERSION 2.8)
project( Wbud )
find_package( OpenCV REQUIRED)
add_executable ( main ../main.cpp )
if(APPLE)
    include_directories(SYSTEM /opt/X11/include)
endif()
target_link_libraries ( main ${OpenCV_LIBS} xdo)
